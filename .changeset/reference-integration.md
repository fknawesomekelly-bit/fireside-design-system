---
"@fireside/tokens": patch
---

Add SSOT MD3 canonical tokens (--md-sys-color-*) via sync-ssot transformer. Doolit: 24 sys.color roles, Aventor: seed-based primary. Existing --color-semantic-* tokens unchanged (Option C parallel emit).
